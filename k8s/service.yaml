apiVersion: v1
kind: Service

metadata:
  name: automation-platform-service

spec:
  selector:
    app: automation-platform

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080

  type: NodePort