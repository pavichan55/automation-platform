apiVersion: v1
kind: Service
metadata:
  name: {{ include "automation-platform-chart.fullname" . }}
  labels:
    {{- include "automation-platform-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "automation-platform-chart.selectorLabels" . | nindent 4 }}
